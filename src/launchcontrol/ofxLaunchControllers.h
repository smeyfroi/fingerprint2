#pragma once

// Vendored from smeyfroi/ofxLaunchControllers (our own fork) and internalized
// into the app so we no longer depend on the addon. Only the Launch Control XL
// (mk3) path is kept; the unused small ofxLaunchControl class was dropped.
#include "ofxLCLeds.h"
#include "ofxLaunchControlXL.h"
#include "ofxLaunchControlXL3Display.h"
#include "ofxLaunchControlXL3Leds.h"
